name: Plan Execution
on:
  workflow_dispatch:
  repository_dispatch:
    types: [plan]
jobs:
  plan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Run Plan
        run: echo "Plan executed"
